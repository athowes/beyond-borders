\documentclass{article}

\pagestyle{empty}

\usepackage[dvipsnames]{xcolor}
\usepackage{tikz}
\usetikzlibrary{backgrounds}
\usetikzlibrary{arrows,shapes}
\usetikzlibrary{tikzmark}
\usetikzlibrary{calc}

\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{amssymb}
\usepackage{mathtools, nccmath}
\usepackage{wrapfig}
\usepackage{comment}

\usepackage{graphicx}

% for custom commands
\usepackage{xspace}

% color box
\usepackage{tcolorbox}

% for tikz
\usepackage{tikz}
%\usetikzlibrary{trees}
\usetikzlibrary{arrows,shapes,positioning,shadows,trees,mindmap}
% \usepackage{forest}
\usepackage[edges]{forest}
\usetikzlibrary{arrows.meta}
\colorlet{linecol}{black!75}
\usepackage{xkcdcolors} % xkcd colors

% for colorful equation
\usepackage{tikz}
\usetikzlibrary{backgrounds}
\usetikzlibrary{arrows,shapes}
%\usetikzlibrary{tikzmark}
\usetikzlibrary{calc}
% Commands for Highlighting text -- non tikz method
\newcommand{\highlight}[2]{\colorbox{#1!17}{$\displaystyle #2$}}
%\newcommand{\highlight}[2]{\colorbox{#1!17}{$#2$}}
\newcommand{\highlightdark}[2]{\colorbox{#1!47}{$\displaystyle #2$}}

% my custom colors for shading
\colorlet{mhpurple}{Plum!80}

\definecolor{lightblue}{HTML}{56B4E9}
\definecolor{lightgreen}{HTML}{009E73}
\definecolor{lightorange}{HTML}{E69F00}

% Commands for Highlighting text -- non tikz method
\renewcommand{\highlight}[2]{\colorbox{#1!17}{#2}}
\renewcommand{\highlightdark}[2]{\colorbox{#1!47}{#2}}

\begin{document}

\begin{equation*}
  \label{eq:besag}
  \tikzmarknode{fc}{\highlight{lightorange}{$u_i \, | \, \mathbf{u}_{-i}$}}
  \sim \mathcal{N}
  \left(
  \tikzmarknode{mean}{\highlight{lightblue}{$\frac{1}{n_{\delta i}} \sum_{j: j \sim i} u_j$}},
  \tikzmarknode{var}{\highlight{lightgreen}{$\frac{1}{n_{\delta i}\tau_u}$}}
  \right)
\end{equation*}

\begin{tikzpicture}[overlay,remember picture,>=stealth,nodes={align=left,inner ysep=1pt},<-]
  % For "fc"
  \path (fc.south) ++ (0,-2em) node[anchor=south west,color=lightorange] (scalep){$i$th full conditional};
  \draw [color=lightorange](fc.south) |- ([color=lightorange]scalep.south east);
  % For "mean"
  \path (mean.north) ++ (0,2em) node[anchor=south east,color=lightblue] (scalep){Average of neighbours};
  \draw [color=lightblue](mean.north) |- ([color=lightblue]scalep.south west);
  % For "var"
  \path (var.north) ++ (0,4em) node[anchor=south east,color=lightgreen] (scalep){Proportional to number of neighbours};
  \draw [color=lightgreen](var.north) |- ([color=lightgreen]scalep.south west);
\end{tikzpicture}

\end{document}
