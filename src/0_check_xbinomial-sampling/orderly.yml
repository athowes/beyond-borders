script: script.R

artefacts:
  - data:
      description: Do the `xbinom` and `xbinom_logit` custom functions work?
      filenames:
        - xbinom-ecdf-comparison.pdf

resources:
  - xbinom.stan
  - xbinom_logit.stan

packages:
  - bsae
  - sf
  - rstan
  - ggplot2
  - bayesplot
  - rmarkdown
